{
  "name": "challan-extractor",
  "description": "Fetch one or more Parivahan eChallan print-page URLs and return a list of structured challan fields plus evidence image URLs for the traffic challan advisor agent.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["urls"],
    "properties": {
      "urls": {
        "type": "array",
        "minItems": 1,
        "description": "https print-page URLs on echallan.parivahan.gov.in (path /report/print-page).",
        "items": {
          "type": "string",
          "format": "uri"
        }
      }
    }
  }
}
