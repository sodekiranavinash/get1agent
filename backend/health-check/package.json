{
  "name": "@get1agent/health-check",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "zip": "cd dist && zip -qX function.zip index.js",
    "package": "npm run build && npm run zip",
    "test": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/rds-signer": "^3.888.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22.18.1",
    "@types/pg": "^8.15.5",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
