[project]
name = "get1agent-migrations"
version = "0.1.0"
description = "Alembic migrations for get1agent (local dev + migration Lambda)"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.14.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "boto3>=1.35.0",
]
