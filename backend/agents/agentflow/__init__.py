"""agentflow — the single-agent runtime (AgentCore container).

Runs one saved agent with the Strands Agents framework and streams normalized
SSE events. Multi-agent workflow orchestration will live in a sibling package
once it lands; for now this package owns the whole container app.
"""
