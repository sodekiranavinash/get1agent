[project]
name = "get1agent-knowledge-mcp"
version = "0.1.0"
description = "MCP server exposing the get1agent knowledge-base tools over HTTP"
requires-python = ">=3.14"
dependencies = [
    "awslabs.mcp-lambda-handler==0.1.15",
    "python-dateutil>=2.8.2",
]
