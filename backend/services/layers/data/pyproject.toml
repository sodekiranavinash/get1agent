[project]
name = "get1agent-layer-data"
version = "0.1.0"
description = "Shared data layer: SQLAlchemy async + asyncpg + shared/db"
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "alembic>=1.14.0",
    "tzdata>=2024.1",
]
