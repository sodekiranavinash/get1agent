[project]
name = "get1agent-layer-data"
version = "0.1.0"
description = "Shared data layer: DynamoDB + S3 search/ingestion code (stdlib + runtime boto3)"
requires-python = ">=3.14"
dependencies = [
    "tzdata>=2024.1",
]
