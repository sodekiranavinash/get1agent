[project]
name = "get1agent-web-search"
version = "0.1.0"
description = "MCP server exposing the get1agent web-search tool (Exa)"
requires-python = ">=3.14"
dependencies = [
    "awslabs.mcp-lambda-handler==0.1.15",
    "python-dateutil>=2.8.2",
]
