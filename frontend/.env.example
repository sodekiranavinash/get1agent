# Base URL for the get1agent API (no trailing slash).
# Production default is used when unset: https://api.get1agent.com
# For local backend testing point this at your deployed API or local proxy.
VITE_API_URL=https://api.get1agent.com
